---
title: Tabs
---

import TabBadges from "./demo/badges.tsx";
import TabIcons from "./demo/icons.tsx";
import PrimaryTabs from "./demo/primary.tsx";
import SecondaryTabs from "./demo/secondary.tsx";
import { SpecLinks } from "../../components/SpecLinks";

```tsx
import { Tabs, TabList, Tab, TabPanel } from "@brijbyte/md3-react/tabs";
```

```css
@import "@brijbyte/md3-react/tabs.css";
```

<SpecLinks
  links={[
    { label: "Base UI Tabs", href: "https://base-ui.com/react/components/tabs" },
    { label: "MD3 spec", href: "https://m3.material.io/components/tabs/overview" },
  ]}
/>

## Primary tabs

The active indicator hugs the label and stretches elastically between tabs, per MDC Android. Arrow
keys move focus and activate.

<PrimaryTabs />

## Secondary tabs

A flatter 2dp indicator spans the full tab width — for a second level of content
within a page.

<SecondaryTabs />

## With icons

Primary tabs stack the icon above the label (64dp); secondary tabs keep it inline.

<TabIcons />

## With badges

```tsx
import { Badge } from "@brijbyte/md3-react/badge";
```

```css
@import "@brijbyte/md3-react/badge.css";
```

Pass `badge` to a Tab: it anchors to the icon's top-right corner, or sits inline after the label
on text-only tabs. Badges are visual — put the count in the tab's `aria-label` too.

<TabBadges />
