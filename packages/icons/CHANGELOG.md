# Changelog — @brijbyte/md3-icons

Each `## v<version>` section becomes the GitHub release body for the
`icons-v<version>` tag; a release cannot be cut without one (`pnpm release`
checks).

## v0.0.1

Initial release: Material Symbols (weight 400) as per-icon React components.
